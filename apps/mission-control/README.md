# @ryuzi/mission-control (planned)

Web app to remotely view and control **all** workdirs and sessions across gateways.

Will connect to the `ryuzi` daemon (`@ryuzi/cli`) over the `ControlPlaneApi` (WebSocket transport via `Bun.serve()`) and consume `@ryuzi/protocol` types. Not implemented yet — reserved as a workspace so the name and seam exist.
