# @harness/ide (planned)

Desktop app (Cursor/Conductor-style) to drive workdirs and sessions locally and remotely.

Will consume `@harness/protocol` and talk to the router over the `ControlPlaneApi`. Desktop shell (e.g. Tauri or Electron) to be decided in its own phase. Not implemented yet — reserved as a workspace.
